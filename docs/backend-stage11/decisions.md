# 阶段 11（融合感知页接真实数据）自动决策记录

| 编号 | 决策 | 理由 |
| --- | --- | --- |
| 11-1 | 只换数据源，不改 HUD 布局与 DOM | 用户约束"不随意改之前的页面，只把数据接到前端" |
| 11-2 | 空域 MultiPolygon 只取每个多边形外环画图，孔洞忽略并记 TODO | `map.js` 只画单环；孔洞在东营数据里少见，先保证不阻塞 |
| 11-3 | 没有 ACTIVE 模式最新研判的目标显示"待确认"，不显示"合法" | 未知不补默认值 |
| 11-4 | "实时视频"按钮禁用并标"未接入"，不删 | 后端无该能力；删按钮属改布局 |
| 11-5 | 目标与告警 5 s 轮询，空域与设备 60 s，卸载清理 | 与阶段 8 融合状态轮询一致 |
| 11-6 | 图层字典改为本页本地常量（与 mock 的 `AIRSPACE_TYPES` 同名同色），不读 `window.MOCK` | 本页去 Mock；`map.js` 的 MOCK 兜底路径不依赖 |
| 11-7 | 悬浮卡三个操作按钮保留元素："转风险监测 →" 保持可用（`#/risk` 深链生产者之一，`U.goto` 机制仍在）；"通知机场/周边"与"派发驱离"禁用并标"未接入"（分别属交接/设备控制，不在本页接线范围）；置信度条 `<span class="bar">` 保留，用 `fusion_confidence` 填充，无值不渲染 | 审查 11 第 2 轮 P1-1：删按钮属改布局，且转风险入口是阶段 9 专门保住的三条深链之一 |
| 11-8 | 图层键与 `mock.js` 的 `airspaceType` 同名同色同图层键（临时管制属 nofly），不按简报的 limit | `map.js` 画空域仍走 `window.MOCK.airspaceType(...).layer`（mock.js 被 legacy 页面全局加载），否则图例勾选与地图隐藏不一致 |
| 11-9 | AOA 方位线与分路置信度本期不做：读接口未透出 `quality.bearing_deg` 与按来源置信度，留为后端补字段的后续项 | 接口缺字段先报领导不自行加；不补默认值 |
| 11-10 | 研判列表按服务端上限 100 分页取全；分页错误不得被 `.catch` 吞掉为"全部待确认" | 静默失败会给出错误但看似安全的结论 |
