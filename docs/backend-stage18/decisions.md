# 阶段 18 决策记录（对照原版演示改回）

依据：用户要求逐页对照原版纯前端演示（`/Users/frank/Desktop/dongying-vue`，5176）与现版（5173），原版更对的改回。对照报告（截图 + 结论）：https://claude.ai/code/artifact/582fac70-809d-4b84-af57-431b4e0e93a6

| 编号 | 决策 | 依据 |
| --- | --- | --- |
| 18-1 | 工作台排序改回"可操作 > 等级 > 接收时间"，终态沉底（由正在改该文件的另一会话带上） | 现版第一条是已结案的误报，右侧"无待办动作"，与工作台的用途相反 |
| 18-2 | 告警内容改回一句人话，来源与模式放小字；"来源置信"卡接回融合置信度；筛选条一行 | 现版正文里"（模拟）"出现两次，来源拼进正文 |
| 18-3 | 合法性页四个方块改回原版口径（今日研判/合法/非法/不可判定）；页签"系统待确认"统一叫"不可判定"；标题换行修掉 | 原版按结论分类的计数对值班员更直接；同一状态两个叫法 |
| 18-4 | 种原版的五个演示角色与 12 个演示用户（双门禁、幂等只升不降）；角色页超管等级显示改对 | 现版只有两个账号两个角色，演示走不了多角色；超管每格显示"查看"与实际不符 |
| 18-5 | 风险事件页筛选收成一行（其余折叠），地图默认全局视角；审计日志模块码补中文、本机地址显示"本机"；飞行计划小地图比例尺位置 | 截图可见的排版与文案缺陷 |
| 18-6（待用户定） | 融合感知页恢复原版右侧"当前追踪目标"与下方"多源融合结果"面板；处置与处罚页改回"案件为主" | 改页面结构，各约 1–2 天，不自动决定 |
| 18-7（转 A） | 设备管理筛选/表列/详情面板按原版恢复；实时监测与接入调测的空态；大屏"飞行监管态势"补未匹配/偏离两根柱；设备页"接收诊断"英文码 | 均为 A 的页面或 A 的快照接口 |
| 18-8 | 不恢复的：顶栏"检索 Command+K"、方块下的说明小字、报表下载、实时视频/轨迹回放按钮 | 没有对应接口或数据，恢复就是摆设 |
| 18-9 | 超级管理员的动作权限在角色页显示"全部"（按实际生效显示），库内 41 行 READ 不补迁移；合法性页三个规则效果指标并入第一格说明行；合法性四格的计数来自研判列表按 `legal_status` 各取一次 total（同窗口同条件），不新增接口 | E2 18.1 实测：`GET /roles/ROLE-ADMIN` 动作行全为 READ 而 `AccessService` 对超管按最高等级放行，照库显示即页面说假话；`rule-effects/summary` 无按结论计数 |
| 18-10 | 演示角色四个、账号 11 个（超管不能造第二个，`SuperAdminIntegrityInitializer` 会拒绝；角色建成非内置以便在角色页查看矩阵）；`AuditLabelsCoverageTest` 从源码扫审计码要求字典有中文（当场补 12 个） | E1 18.2 |
| 18-11 | 演示角色的动作权限按"菜单 → 该页所需读动作"表推出最少读动作（补动作码，不砍菜单）；审计员补审计日志菜单与审计读/导出动作；用例对每个角色逐菜单断言 | 助手 118 条矩阵 59 红：四个新角色重犯 15-34（菜单有、页面读接口 403，/workbench/items 403 ×207）；审计员按种子进不了审计日志 |
| 18-12 | 值班员补 `disposal:read`（工作台/告警页显示反制与干扰状态）；演示复核员补 `evidence:read`（处罚页案件证据）；审计日志页对无 `roles.read` 的角色不拉 `/roles`，角色筛选取审计日志自身的值或隐藏（与 15 的 `/districts` 同形） | 助手全角色矩阵 114/118 后剩下的四条，全是"页面去取该角色读不到的东西" |
| 18-13 | 审计日志的读与导出（`audit` 模块）允许授予非超管角色；`SuperAdminIntegrityInitializer` 只把 users/roles/countermeasure 保持为超管专有，不按角色名豁免 | 审计员按"只读 + 审计日志导出"定义必须能看审计日志；阶段 4 把 audit 一并收成超管专有是规则过严；E1 提议的 `NOT LIKE ROLE-DEMO-%` 豁免会在产品代码里留下按名字开的洞（生产里叫这个名的角色照样绕过） |
| 18-14（用户裁定） | 飞行风险的流程到"通知上级"为止，回执结果为"已驱离"即闭环；风险不进入处置授权，不出现"处置/处罚"措辞。通知上级弹窗不选接收方（接收方由系统按风险通知的配置自动取），按钮叫"提交通知" | 用户 2026-09-10："风险到通知上级就完事了，只要回执是已驱离就好，到处置就不对" |
| 18-15（用户裁定） | 合法性研判页：删去"复核历史与告警"页签里的告警关联/来源模式/规则集版本·参数/发布模式信息行；删去底部"转告警"按钮（流程里没有转告警）；删去左栏标题旁的规则/刷新/重算三个小按钮 | 用户 2026-09-10 截图指令 |
| 18-14 补充 | 处置授权的主体种类去掉 RISK（服务端 `DisposalRules.SUBJECT_KINDS`），拿风险 id 申请仍答 400 `SUBJECT_KIND_NOT_SUPPORTED`，答复与之前一字不变；库 CHECK 不动以保留历史数据可读 | **E1 更正自己的上报**：后端并非"允许拿风险 id 申请"——入口早就 400 了，只是主体名单上还挂着 RISK，让读代码和读契约的人以为它可用。改的是名单的自我说明，不是行为。答复没有落成泛泛的"类型无效"，是因为前端对这个码有一句能读懂的话（"本期只能对已核实的无人机事件或目标发起处置授权"），换成校验错误反而是退步 |
| 18-16 | 风险通知不传接收方时的解析顺序：默认接收方 → 该类型唯一一个启用的接收方 → 否则 400 `RECIPIENT_REQUIRED`；本地种子把"民航监管部门"标为默认 | E1：生产不自动插接收方也没有配置界面，只按"默认"判会让生产上风险通知一律 400；只有一个接收方时没有可选余地，直接用它语义成立 |
| 18-16 | 风险通知不传接收方时的回落顺序：标了默认的那个 → 该类型恰好只有一个启用接收方就用它 → 零个或多个且无默认才 400 `RECIPIENT_REQUIRED` | 生产没有配置接收方的界面，契约又写"生产不自动插入接收方"；18-14 之后页面不再传接收方，只认默认标记的话生产库上风险通知会一路 400，除非有人直接写库。目录里只有一个启用接收方时本就没有可选余地，让服务端替他决定不会决定错 |
| 18-14 补充二 | 风险通知的回执为"已驱离"时，源风险推进到"已通知"（沿用既有通知完成态）；"未驱离"保持待通知。交接列表 DTO 带 `receipt_result` | E2 联调：回执已 DISPERSED 而风险仍"待通知"，与"回执已驱离即闭环"不符；列表没有该字段，通报记录显示不出结果 |
| 18-14 补充二 | ① 交接列表 DTO 补 `receipt_result`（原来只有详情有）；② 风险通知回执为 `DISPERSED` 时把源风险从"待通知"推进到"已通知"，沿用既有状态、不新造；`NOT_DISPERSED` 与未接通渠道保持"待通知" | E2 对着本地服务联调发现：通报记录列表显示不出"已驱离"；通知已送达已签收已驱离之后风险仍挂在"待通知"，值班台一直把它当未办事项，与 18-14"回执已驱离即闭环"相悖 |
| 18-13 补充 | `SystemManagementService.PROTECTED_CUSTOM_PERMISSIONS` 同步去掉 `audit`（users/roles/countermeasure 保留），管理员可在角色矩阵里授审计 | 审查终审 P1-1：初始化器放开而写接口仍拒绝，演示审计员的矩阵整组提交会被整次拒绝、页面上改不动（与 15 的 P2-3 同形） |
