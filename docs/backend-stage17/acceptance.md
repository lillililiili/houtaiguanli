# 阶段 17 验收记录：验收材料

日期 2026-09-09。基线 `e5efed5`。产出 `docs/交付/` 六份正文 + `docs/交付/内部依据/` 六份依据 + `README.md`。无代码、迁移、测试改动。

## 执行者报告
- 17.1/17.4（E2）：甲方验收用例（按会议纪要十四个页面 + 三条闭环）、操作说明（值班员按班次）。核对源码时改掉四处按常识会写错的地方（工作台五个方块及类别名、告警页六个指标、"尚无引擎研判"只是注释不是文案、统计页并未标注模拟数据）。四处待确认（纪要"十个页面"与十四页口径、公安信号干扰三项字段、合法性页"转入处置"卡点、统计页是否标模拟）。8081/8082 任务期间无进程，核对只用只读接口。
- 17.2/17.5（E1）：协议缺口矩阵（协议 A 探测数据 / 协议 B 控制指令 / 协议 C 光电事件三张表 + 口径类阻塞五条）、联调日志模板（八路各一张表 + 取报文方法）。查证发现三处协议矛盾：协议 C `CameraStatus` "支持"与"暂不支持"自相矛盾；协议 A 与 C 的垂直速度 Z 轴方向相反；三份协议对高度基准说了三种。四条"协议未定义"（无精度字段、目标 id 跨帧稳定性、光电类别取值表不完整、设备类型表无融合箱）。协议 C 只定向读了 1、2、2.3 节，未读部分不下结论。
- 17.3/17.6（助手）：口径说明书（尚未接入 29 处 = 界面 21 + 注释 8；不可判定 13；前端可用性码 27）、安装与升级记录（空库与升级库版本化迁移均 61 步；72 与 70 的差额全部来自可重复脚本重复执行；`uav_stage15_fresh` 已被后续升级，不再是阶段 15 快照，改用 `uav_stage16_fresh` 作空库代表；生产环境尚未进行、更早阶段无记录如实写明）。

## 领导核对
- 禁忌扫描（`.java/.vue/.js`、迁移号、决策号、全大写码、uuid）：六份正文 0 命中；联调日志模板第九节保留 4 条查询语句，读者是我们的联调工程师，属操作步骤不属依据（E1 判断，领导同意）。
- 每份正文开头有"内部依据见 docs/交付/内部依据/"，六份依据文件齐备，锚点为文件 + 方法/常量/协议小节，无行号。
- 协议 A PDF 抽核（领导直接读 v8.6 第 1–8 页）：AOA "目标的经纬度和高度无效，使用方位角"、"30s 收不到设备工参数据则认为设备离线"、`speedZ` "垂直地面向上"、`altitude` 海拔与 `height` 距地两字段、探测数据无任何精度字段、设备类型表含 dec/ifr/bsc 且无"融合箱"——六条与矩阵一致。协议 B/C 未由领导复核（C 为 55 MB，E1 定向读）。
- 审查第 1–3 轮：P1 1 项（E2 两份未按 17-1 拆依据表，已闭合）、建议 4 条（依据表位置、引用原句、重复矩阵文件、"待确认"术语），均处置。审查者端到端复算八条定量主张全部一致。

- 反制四通道控制器抽核（领导读厂家《网络控制器通信协议v2.0.txt》与 `integration/device/countermeasure/Countermeasure4ChCodec`）：帧格式 `0x55 地址 功能码 数据4字节 校验和`、功能码 0x10 查询 / 0x11 断开 / 0x12 吸合 / 0x13 命令执行、迫降=四路全开(0x0F)、驱离=除 1.5G 外全开(0x0D)、ch1–ch4 对应 900M/1.5G/2.4G/5.8G——与矩阵第五节一致；代码侧只实现 0x10 查询（`FUNCTION_QUERY`，注释"只暴露无副作用的 0x10 状态查询"），开/关/掩码未实现——与"不发任何字节"一致。内部依据里的 `0x21 延时断开` 不在 v2.0 txt 中，出处待 E1 补（可能来自《四通道打击数据协议.doc》）。

## 交接给后续的两条（E1 旧稿转交）
- 设备页「接收诊断」把英文码直接显示到屏幕上（形如 `REJECTED · UNSUPPORTED_TYPE`），前端文案，待安排。
- 光电「抓拍/录像」此前盘点为"已建"是误判（匹配到的是 Java 关键字），交付版按"三份协议都未定义图像回传、靠人工上传"为准。
- 合法性页 `UNDETERMINED` 在结论栏叫"不可判定"、页签叫"系统待确认"，口径说明书已解释两者关系；是否统一用词待定（该页在另一会话工作集内，本轮不动）。

## 未完成 / 局限
- 协议 B、C 中以 PDF 为唯一出处的说法未经第二人复核。
- 验收当天的数据量与编号要在验收环境重新确认（文档正文已注明）。
- 面向对接方的《接口取值说明》未做（无人要）。
