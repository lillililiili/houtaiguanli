# 后端开发基线

2026-09-16 报表管理：新增五类按业务记录聚合的即时预览、分页明细及 Excel/PDF 导出；周期与类型分离，复用来源模块读取权限和范围。旧运行统计接口和样本口径保持兼容。无历史快照、定时任务或新增表，详见[运行统计接口契约](运行统计接口契约.md)第 4 节。

2026-09-14 设备实时监测补充：监测页新增与调测同源的完整设备信息，提供独立 `monitoring.read` 查询入口，保持设备数据范围与连接配置脱敏规则；无需新增表或采集通道。页面随当前设备刷新，保留字段来源、缺失、过期与厂家资料边界，详见 [设备运维接口契约](设备运维接口契约.md)。

2026-09-14 后端迁移：已将旧工作区后端修改及缺失的 `modules.target` 迁入当前仓库，修复源码忽略规则。新后端 local 使用 8081 和独立 `houtaiguanli` 库，管理端不再复用旧 8080 进程。具体合并依据、版本映射和验证见 [新后端迁移记录](新后端迁移记录.md)。此前设备调测文档中“缺少 target 模块导致无法整体编译”是迁移前记录，当前已解除。

2026-09-14 设备调测补充：新增只读完整信息接口，按协议组装 245 项字段定义、缺失与时效状态，扩展雷达双寄存器采集并保存完整光电心跳。具体范围、资料差异、变更清单及验证限制见 [设备调测信息覆盖与验证](设备调测信息覆盖与验证.md)；实机逐项获取尚待现场接入确认，不能按厂家标称值判定已采集。

> 最新用户确认：提交成功为已通知，确认回执后为已回执；此前通知口径以 [风险通知状态口径](风险通知状态口径.md) 为准。

版本：V1.0；建立日期：2026-09-03。

2026-09-14 设备删除：管理端新增停用后逻辑删除，保留设备身份和历史关联，校验操作权限、组织/区域范围、版本与进行中任务，并同步台账和监控列表过滤。接口与验证边界见[设备运维接口契约](设备运维接口契约.md)。前端和隔离 PostgreSQL 校验已通过；完整后端编译被既有 `modules.target` 源码缺失阻塞，未声明端到端验收通过。

2026-09-10 MQTT 联调修复：新增授权来源继承、无人机规则类别守卫、回执驱动风险通知状态、MQTT 心跳异常与恢复核验、授权证据链补全及 C04 主类别识别。具体运行证据和边界见 [MQTT 问题修复与复测结果](MQTT问题修复与复测结果-20260910.md)，手工操作见 [MQTT 手册](MQTT手动测试操作手册.md)。

本文件是当前仓库后端开发的业务与技术起点。配套执行规则见 [server/AGENTS.md](../server/AGENTS.md)，启动与检查入口见 [后端 README](../server/README.md)。甲方对照验收见 [验收材料](验收材料/README.md)（用例、协议缺口、口径、操作说明、联调日志模板）；写了验收材料不等于现场联调完成。2026-09-03 已落地身份权限和设备运维开发切片；设备协议实联、生产数据范围及正式调测判据仍未完成。

数据库字段、表关系、约束与分期迁移的设计细化见[数据库设计文档](数据库设计文档.md)。设备运维的已实现 REST 约定见[设备运维接口契约](设备运维接口契约.md)。

## 1. 适用范围与依据

- 在同仓库 `server/` 内渐进开发，前端保留在 `dongying-vue/`，部署配置保留在 `deploy/`。不另建后端仓库，不重搭框架。
- 首期承接当前已确认的三条业务链；责任人领取、转派、SLA、完整行政处罚审批、多源融合算法研发等不是默认交付内容。
- 当前事实由源码描述，业务口径由已明确确认的需求约束；前端代码是业务资产，不是生产协议。发现冲突必须记录、确认，不静默选择一方。
- [事件驱动业务流程梳理](事件驱动业务流程梳理.md)用于理解方向，其中未确认的路线图能力不能自动成为本期字段或流程。
- 当前工作区已删除的旧后端技术方案和整体改造方案不作为依赖，不恢复它们；不依赖仓库中不存在的编号文档。

全文使用以下四种状态，可同时标记同一模块的不同能力：

| 标记 | 含义 | 使用限制 |
| --- | --- | --- |
| A：已有代码 | 仓库可定位到实现 | 本基线中的 A 均仅经静态核查，未执行运行验证；不得等同生产可用 |
| B：已确认待开发 | 现有业务边界已确认，后端能力待实现 | 进入具体开发前补齐该切片的接口、数据和验收设计 |
| C：演示实现 | Mock、浏览器内存、模拟回执或演示文件台账 | 只作流程参考或隔离测试夹具，不作真实执行证据 |
| D：待确认 | 业务规则、来源协议或部署条件缺少确认 | 记录问题和影响；不得用虚构数据填补 |

## 2. 技术与接口兼容基线

### 2.1 当前技术组合

| 项目 | 基线 | 源码依据 |
| --- | --- | --- |
| Java / Spring Boot | Java 17；Spring Boot 3.4.5 | [pom.xml](../server/pom.xml) |
| 数据访问 | MyBatis Spring Boot Starter 3.0.4，现有 Mapper 使用注解 SQL | 同上；`modules/identity/infrastructure` |
| 构建 | Maven Wrapper，Maven 3.9.9 | [Wrapper 配置](../server/.mvn/wrapper/maven-wrapper.properties) |
| 数据库迁移 | Flyway，依赖版本随当前 Boot 依赖管理；已有 V1、V2 | [迁移目录](../server/src/main/resources/db/migration) |
| 业务数据库 | PostgreSQL 16 / PostGIS 3.5；部署示例镜像 `postgis/postgis:16-3.5` | [Compose](../deploy/compose.yml)；尚无空间业务迁移 |
| 安全与运维 | Spring Security、BCrypt、自有 Bearer 会话、Actuator | `platform/config`、`modules/identity` |
| 测试 | Spring Boot Test、MockMvc、H2 PostgreSQL 兼容模式 | [测试配置](../server/src/test/resources/application-test.yml) |

这是用户确认沿用的工程组合，不是“最新版本”推荐或生产安全认证。新增依赖、版本升级、Redis、消息中间件、对象存储服务或其他基础设施，需单独确认，不把它们变成首期启动前提。

### 2.2 已有公开接口

| 方法与路径 | 当前请求/结果 | 状态 |
| --- | --- | --- |
| `POST /api/v1/auth/login` | `account/password`；返回用户摘要、`role_code/session_id/expire_at` | A |
| `POST /api/v1/auth/logout` | Bearer 会话失效 | A |
| `GET /api/v1/auth/me` | 当前用户摘要、角色码、`source_mode` | A |
| `GET /api/v1/alarms` | `page/size`；固定空分页 | A，占位查询 |
| `GET /api/v1/devices` | 服务端分页、筛选和白名单排序，返回台账与最新状态摘要 | A，设备运维切片 |
| `/api/v1/device-monitor/*` | 总览、设备树、最新状态、历史曲线与事件增量查询 | A，mock/live 同一 REST 契约 |
| `/api/v1/devices/{id}/commands/reboot` | 幂等异步重启；回执、超时与状态查询 | A，当前仅 mock |
| `GET /api/v1/device-incidents/{id}` | 异常摘要、重启指令、最近校验、`allowed_actions` | A |
| `POST /api/v1/device-incidents/{id}/reboot` | 挂到异常单的重启；仅 PENDING；202 | A，live 协议未声明重启则 409 |
| `POST /api/v1/device-incidents/{id}/recovery-checks` | 平台状态快照校验；PASS 才关闭 | A |
| `/api/v1/commission-tasks/*` | 创建、连接、配置、启动、取消、事件与在线报告 | A，当前仅 mock |
| `GET /api/v1/stats/operations` | 运行统计近区间聚合：KPI、分日/区域/类型/风险/高度/时长/轨迹、处罚构成与主体排行；设备在线数取自设备台账 | A，样本事实表，口径待 Q6 |
| `GET /api/v1/stats/operations/export.csv` | 与查询同口径、同范围的 CSV 导出；UTF-8 BOM；成功留审计 | A，样本事实 |
| `GET /api/v1/stats/reports/preview` | 按上海时区生成日报、自然周报、自然月报即时预览；当前周期截止当天 | A，样本事实表，口径待 Q6 |
| `GET /api/v1/stats/reports/export.xlsx` | 与预览同快照和数据范围的四工作表 XLSX；成功留审计 | A，样本事实 |
| `GET /api/v1/dashboard/snapshot` | 数据大屏只读快照：按源权限聚合今日 KPI、趋势、设备、计划、告警与地图点；缺权计数为 null | A，聚合已有领域表；趋势仍用统计样本事实 |
| `/api/v1/evidence-files*` | 证据文件摄取、列表详情、授权下载、关联、校验、冻结、平台缺省留存期、人工销毁与 CSV | A，文件底座；自动销毁仍 D |
| `GET /api/v1/evidence-chains/{subject_kind}/{subject_id}` | 按事件/目标汇总八类记录、链完整性校验值、缺失标志、目标 ID 变更回溯 | A，只读聚合；不自存文件；销毁仍 D |

依据：[AuthController](../server/src/main/java/com/uav/lowaltitude/modules/identity/api/AuthController.java)、[AlarmController](../server/src/main/java/com/uav/lowaltitude/modules/alarm/api/AlarmController.java)、[DeviceController](../server/src/main/java/com/uav/lowaltitude/modules/device/api/DeviceController.java)、[ReportingController](../server/src/main/java/com/uav/lowaltitude/modules/reporting/api/ReportingController.java)、[DashboardController](../server/src/main/java/com/uav/lowaltitude/modules/dashboard/api/DashboardController.java)、[EvidenceController](../server/src/main/java/com/uav/lowaltitude/modules/evidence/api/EvidenceController.java)、[EvidenceChainController](../server/src/main/java/com/uav/lowaltitude/modules/evidence/api/EvidenceChainController.java)。运行统计字段级约定见[运行统计接口契约](运行统计接口契约.md)；大屏快照见[数据大屏快照接口契约](backend-stage8/dashboard-api-contract.md)；证据文件见[证据关联服务契约](backend-stage9/evidence-api-contract.md)；证据链见[证据链契约](backend-stage9/evidence-chain-api-contract.md)。

- 保留 `/api/v1` 和 `Authorization: Bearer <session_id>`；当前 token 是持久化会话 ID，不是 JWT。不得把前端用户 ID 当后端登录凭证。
- 保留 [ApiResponse](../server/src/main/java/com/uav/lowaltitude/platform/api/ApiResponse.java) 的 `ok/data/error` 结构，错误内含 `code/message`；错误应使用匹配的 HTTP 状态，不能把失败包装成业务成功。
- 当前 [Jackson 配置](../server/src/main/resources/application.yml)为 snake_case 和忽略 null；既有接口不得全局改名或更改空值表现。新接口须明确缺失、未知、不适用的区别，需要显式 null 时在对应 DTO/契约中处理，不能默认当前配置会保留它。
- 列表当前返回 `items/page/size/total`，默认第 1 页、每页 20 条，页码至少 1、每页 1–100 条。新增列表复用结构，筛选排序等在具体接口中明确。
- 现有 ID 为字符串，登录过期时刻为 epoch 毫秒；不擅自转换既有线格式。新增接口明确时间单位、时区及状态字典，不混用秒与毫秒。
- 本轮只列接口能力，不定义全部新 URL、字段级 OpenAPI 或建表 SQL；这些随业务切片设计，不一次性猜出整套契约。

## 3. 当前实现盘点

| 能力 | 当前代码与限制 | 状态 |
| --- | --- | --- |
| 认证 | 数据库存用户和会话，前端登录、刷新恢复、退出和 401 清理均调用后端 | A；生产身份源仍待确认 |
| 权限 | 唯一内置超级管理员权限固定；自定义角色提供菜单和动作权限；设备连接敏感字段按 `devices.op` 控制。用户管理不再维护数据范围，新建用户默认 `ALL` | A；生产组织范围仍待 Q3 |
| 身份数据表 | `app_user/app_session/audit_log` 已由后续迁移扩展，并新增组织、区域、角色、权限和数据范围关系 | A |
| V2 数据表 | `outbox_event/inbox_message/idempotency_request`；尚未形成业务发送、回执消费和请求幂等闭环 | A/B |
| 审计 | [AuditMapper](../server/src/main/java/com/uav/lowaltitude/platform/audit/AuditMapper.java)只有 INSERT；服务写登录等审计。Mapper 只增不等于数据库权限、保管及防篡改措施已完成 | A/B |
| 设备运维 | V3 建立台账、连接配置、最新/历史状态、异常、事件、命令回执与调测任务；三张前端页面读取同一后端 | A，运行库不再预置模拟台账；协议实联 D |
| 外部适配 | `DeviceAdapterRegistry` 按模式+协议路由；已有 `MockAdapter`、雷达 TCP v3.0.0 只读 Adapter、四通道控制器 v2.0 查询与继电器设置 | A；协议模拟器已验证，尚未完成甲方现场射频联调 |
| 后台任务 | [OutboxWorker](../server/src/main/java/com/uav/lowaltitude/platform/worker/OutboxWorker.java)持久化领取设备 Outbox，支持退避重试、命令超时和重启后恢复扫描 | A，当前处理设备主题 |
| 文件存储 | [LocalObjectStorage](../server/src/main/java/com/uav/lowaltitude/platform/storage/LocalObjectStorage.java)写入时计算 SHA-256；证据业务见 `modules/evidence` | A；平台缺省留存年限 A，人工销毁 A，自动销毁仍 D |
| 测试 | [AuthApiTest](../server/src/test/java/com/uav/lowaltitude/modules/identity/api/AuthApiTest.java)覆盖登录、错误密码、会话、退出和空列表鉴权；运行统计见 [ReportingApiTest](../server/src/test/java/com/uav/lowaltitude/modules/reporting/api/ReportingApiTest.java) | A；由普通 `./mvnw test` 发现 |
| 运行统计 | `GET /api/v1/stats/operations`、`.../export.csv` 与 `GET /api/v1/stats/reports/*` 从 `report_airborne_target`/`report_penalty_case` 聚合，设备 KPI 读设备台账；`local`/`test` 由 Seeder 写入样本事实 | A，样本数据 C，正式口径 D |

设备运维表、live 适配器和开发模拟闭环已经存在。`local` 不再写入 `DEV-MOCK-*` 台账；模拟设备只作为 `test` profile 夹具。飞行计划、轨迹、证据等其他正式业务表仍未按本设计落地；live 握手失败不得解释为甲方设备已经验收。

### 3.1 协作者 B 阶段 7–9 与 8.5 交付事实（截至 2026-09-07，提交 `2ef0fc5`）

| 能力 | 当前代码与限制 | 状态 |
| --- | --- | --- |
| 规则引擎与合法性（阶段 7） | `modules/assessment/engine`：规则集/版本/参数（`rule_set`、`rule_set_version`、`rule_param`，全部 DEMO）、C01 计划匹配、C02-1…C02-8、C03 决策表、C06 告警合并；`RuleEngineWorker` 单行租约调度（`app.rule-engine.enabled` 缺省关）；`assessment_result` 只追加；十场景回放 `RuleReplayRegressionTest` + `Stage7PostgresTest` | 已交付；阈值与权重未经业务方确认 |
| 融合引擎（阶段 8） | `modules/fusion`：`inbox_message` → `FusionIngestWorker` → `FusionPipeline`（α-β、门限+匈牙利关联、ID 状态机、属性优选、加权融合、降级）→ 阶段 2 目标库 + `track(layer FUSED)`、`target_lineage`、`target_current_alias`、`fusion_event`；`fusion_config demo-v1` 参数全部 DEMO；写接口修订类别/合并/分裂带幂等键与 `expected_version` | 已交付；自动合并/分裂不在管线内触发 |
| 设备直连切片（阶段 8.5） | `modules/fusion/ingest`：`InboxSourceRouter` 按 `source` 前缀分派 `replay:`/`lingyun:`/`eo-edge:`/`live-radar:`；凌云协议 A `SenseData`、协议 C `BeginTracking` 映射为 `SourceObservation`（光电/AOA 位置置空、255 识别中类别为空、高度只落 `quality.altitude_raw`）；`source_observation.pilot_location/class_source`、`target_latest_state.pilot_location/pilot_observed_at`；C02-6 超视距按飞手位置判定；实测雷达提升端口 `LiveRadarSourceObservationPort`（`app.fusion.live-promotion.enabled` 缺省关）；`inbox_message.ingest_seq` 确定性领取；来源目录八行仅 RADAR CONFIRMED。A 侧 2026-09-08：协议 A 登记/信封扩到 `aoa/dcd/rid`；光电手点 `POST /targets/{id}/eo-tracking-tasks`，自动跟踪默认关；雷达/反制原生协议不改 | 已交付；高度基准、各源精度、MQTT broker 部署与厂家原生替换待客户/厂家 |
| 空域与空间风险（阶段 9） | 空域写模型与接替式版本（只允许 `valid_to` NULL→非 NULL）、GeoJSON 导入批次、C04 空中异物决策表（`SPACE-RISK-DEMO`，`SpaceRiskEvaluationJob` 缺省关）、机场骨架（`airport*`）、飞行计划实际对照（外部授权登记已于 2026-09-09 按需求确认表增补四 F8 整体撤除：`V202609090106` 删表与 `flight:authorize` 权限）；`airspace`/`risk` 菜单提升已由用户撤回（`V202609070010`），其余动作权限与接口保留 | 已交付；真实鸟类识别、驱鸟、机场通报通道未接入 |
| PostgreSQL 专项 | `Stage4/5/7/8/85/9PostgresTest` 在真实 PostgreSQL 16.9 + PostGIS 3.5.2 上跑（环境变量门禁，库名 `stage456_verify_*`；`TargetReadPostgresApiTest` 需 `stage2_target_verify_*`），`db/postgresql/R__*` 提供只增触发器、几何 CHECK、GIST | 持续 |
| 决策与验收记录 | `docs/backend-stage7/8/85/9/decisions.md` 与 `acceptance.md`；契约 `docs/backend-stage8/device-direct-access-contract.md` v1.2 | 持续 |

## 4. 业务模块与接口能力清单

### 4.1 前端参考入口

以下短码供模块表引用，指向当前仓库的真实资产；读取时同时区分 Vue 入口与 legacy 实现，不迁移页面结构来替代后端开发。

| 短码 | 参考入口 | 提取内容 |
| --- | --- | --- |
| F1 | [mock.js](../dongying-vue/public/assets/js/mock.js) | 数据关系、字典、`PERM/MENU_PERM`、状态与推导规则；随机值及待确认参数不可直接迁入生产 |
| F2 | [case.js](../dongying-vue/public/assets/js/case.js) | 目标关联、人工核实、处置推进、联动动作与证据关联 |
| F3 | [workbenchEvents.js](../dongying-vue/src/services/workbenchEvents.js) | 三类事项统一列表、详情、入口与设备异常内存状态 |
| F4 | [auth.js](../dongying-vue/src/services/auth.js)、[accessControl.js](../dongying-vue/src/services/accessControl.js) | 后端会话恢复、菜单及动作权限入口；系统管理页面不再使用 Mock 账号和权限 |
| F5 | [monitor.js](../dongying-vue/public/assets/js/pages/monitor.js)、[deviceRebootModal.js](../dongying-vue/src/ui/deviceRebootModal.js) | 设备监测、重启原因、指令与模拟回执 |
| F6 | [risk.js](../dongying-vue/public/assets/js/pages/risk.js)、[FlightsPage.vue](../dongying-vue/src/pages/FlightsPage.vue) | 风险核验、排除、通知及 `RISK_IMPL` |
| F7 | [counterAuthModal.js](../dongying-vue/src/ui/counterAuthModal.js) | 联动授权参数和确认项；当前回调未传递全部选择参数 |
| F8 | [EvidencePage.vue](../dongying-vue/src/pages/EvidencePage.vue)、[video.js](../dongying-vue/public/assets/js/video.js) | 文件台账、证据查看和演示视频，不是实际文件或流 |
| F9 | [SituationPage.vue](../dongying-vue/src/pages/SituationPage.vue)、[LegalityPage.vue](../dongying-vue/src/pages/LegalityPage.vue) | 目标查询、合法性依据与事实修订 |
| F10 | [DevicesPage.vue](../dongying-vue/src/pages/DevicesPage.vue)、[CommissionPage.vue](../dongying-vue/src/pages/CommissionPage.vue) | 设备台账、接入及调测结果 |
| F11 | [AlarmsPage.vue](../dongying-vue/src/pages/AlarmsPage.vue)、[PunishPage.vue](../dongying-vue/src/pages/PunishPage.vue) | 核实、处置与交接入口 |
| F12 | [StatsPage.vue](../dongying-vue/src/pages/StatsPage.vue)、[ArchivePage.vue](../dongying-vue/src/pages/ArchivePage.vue) | 统计口径、日志检索与归档展示 |

### 4.2 对象、查询和动作

保留现有 `identity/alarm/device` 包。其他名称是逻辑模块边界，不表示目录已经建立；按实际切片创建，不生成空模块。下表 B 表示承接现有业务能力，不表示所有字段、阈值或外部协议已确认。

| 模块 | 对象与关联 | 查询能力 | 写入动作 | 依据与状态 |
| --- | --- | --- | --- | --- |
| 身份权限 identity | 用户、组织范围、自定义角色、会话、授权关系 | 当前用户、用户角色列表、菜单及动作权限 | 登录退出；超级管理员直接维护用户、范围与角色权限 | F1/F4；认证与系统管理 A，生产组织资料 D |
| 设备 device | 台账、最新状态、状态历史、每次异常、调测记录 | 设备列表详情、健康与异常历史、调测结果 | 台账维护、状态接收、异常确认、恢复验证；重启委托指令模块 | F1/F5/F10；空接口 A，演示 C，流程 B，协议 D |
| 感知目标 sensing | 来源目标、平台目标、轨迹和来源关联 | 实时目标、历史轨迹、来源质量 | 接收并归一化上报、来源范围内去重；不默认自研融合算法 | F1/F9；演示 C，持久化与查询 B，协议和合并规则 D |
| 飞行计划与空域 flight | 计划、航线、空域及其版本 | 计划详情、关联航线、空域与有效期 | 同步/维护计划和空间数据、保存版本；权威来源待定 | F1/F6；演示 C，承接 B，数据主责 D |
| 研判 assessment | 事实输入、规则版本、研判结果及人工修订 | 合法性与风险依据、未知原因、历史修订 | 按已确认规则计算、人工修订留痕、重算受影响结果 | F1/F9；确定性规则承接 B，演示 C，生产阈值 D |
| 告警处置 alarm | 告警、核实、无人机处置记录、目标关联 | 告警队列、详情、阶段和可用动作 | 属实/误报核实、发起联动用例、依据回执推进处置 | F1/F2/F7/F11；空接口 A，演示 C，主线 B，真实控制条件 D |
| 风险 risk | 风险事件、航线/计划关联、核验、通知 | 风险列表详情、等级依据、关联计划 | 核验通过、排除、通知上级 | F1/F6；状态迁移 B，演示 C，阈值与渠道 D |
| 指令回执 command | 授权、逻辑指令、发送尝试、执行回执 | 指令进度、结果、历史回执 | 校验并下发、接收回执、超时处理；停止/急停依真实协议确认 | F2/F5/F7；控制可靠性 B，定时器 C，协议与授权 D |
| 交接通知 handoff | 内部交接、发送记录、送达与外部回执 | 通知记录、交接进度、接收结果 | 通知处罚部门、通知上级、记录发送与接收结果 | F1/F6/F11；交接边界 B，浏览器通知 C，接收系统 D |
| 证据 evidence | 实际文件、元数据、业务关联、访问记录 | 列表详情、关联业务、受控下载与文件哈希校验；按事件/目标汇总八类记录与链校验值 | 接收入库、计算哈希、关联、冻结；按种类写入平台缺省 `retain_until`；已到期且未冻结可人工销毁 | F1/F8；文件底座 A，台账接 API，C07 证据链 A（只读聚合），平台缺省留存 A，人工销毁 A，自动销毁/正式档案制度 D |
| 工作台 workbench | 事件实例索引、源业务关联、派生摘要 | 三类待办、详情、关联时间线与统计 | 不直接写源状态；动作委托各业务模块 | F2/F3；聚合 B，内存事项 C，分派/SLA 不纳入本期 |
| 统计审计 reporting/audit | 业务记录、统计结果、操作及访问审计 | 按权限范围查询统计、日志与导出结果 | 服务端产生审计、按真实数据生成统计/导出 | F1/F12；基础审计 A，运行统计查询与 CSV 导出 A（样本事实），正式指标 D |

### 4.3 来源、权限与阻塞资料

问题编号对应第 7 节；所有模块均须执行接口授权和数据范围控制，不能仅复用前端隐藏按钮的效果。

| 模块 | 真实来源 | 权限、审计重点 | 待补资料 |
| --- | --- | --- | --- |
| identity | 确认后的人员组织资料或身份系统 | 唯一超级管理员维护用户和菜单/动作授权；权限变更立即撤销受影响会话 | Q3/Q8 |
| device | 设备台账、心跳、状态上报、平台验证结果 | 区域/设备范围；维护、重启、恢复分别授权留痕 | Q1/Q2/Q3/Q5/Q6 |
| sensing | 设备或既有融合平台报文 | 来源与区域范围、敏感标识；接入异常与关联变更可追溯 | Q1/Q2/Q6/Q8 |
| flight | 确认后的计划平台、航线空域资料或授权人工维护 | 数据所有权、维护权限、版本变化及来源同步留痕 | Q2/Q3/Q4/Q6 |
| assessment | 真实事实输入、确认规则、授权人工修订 | 修订权限；留存输入依据、修订人和规则版本 | Q2/Q3/Q6 |
| alarm | 设备告警/规则事件、人工核实、指令与交接结果 | 核实与受控处置分权；阶段、证据、审计保持一致 | Q1/Q2/Q3/Q5/Q6 |
| risk | 目标、计划航线、确认规则与人工核验 | 核验/排除/通知授权；结果与依据、通知记录关联 | Q2/Q3/Q4/Q6 |
| command | 服务端授权上下文及认证过的设备回执 | 绑定对象、设备和参数；重试不绕过授权，回执不可由用户伪造 | Q1/Q2/Q5 |
| handoff | 内部接收模块、外部接收系统及其回执 | 接收方和材料范围；发送失败与成功分别留痕 | Q3/Q4/Q7 |
| evidence | 真实文件、媒体平台、原始报文和业务材料 | 查看下载导出授权；文件访问、保管及完整性异常留痕 | Q1/Q4/Q7/Q8 |
| workbench | 各模块已持久化的真实状态 | 聚合前过滤数据范围；不得通过摘要泄露无权访问的事项 | Q3；后续任务化另行确认 |
| reporting/audit | 权限范围内的业务记录和服务端审计 | 统计/导出不能越权；敏感日志脱敏；保留访问记录 | Q3/Q6/Q7/Q8 |

## 5. 业务状态与数据边界

### 5.1 三条流程独立建模

1. 无人机：告警触发 → 人工核实 → 一次人工发起联动反制 → 系统自动执行信号干扰 → 通知处罚部门。保留误报分支；联动是同一共享用例，实际下发和推进必须满足确认后的授权条件并依赖真实回执。交接完成不等于处罚办结，不增加手工归档环节，也不把 Mock `cases` 全量认作正式行政案件。
2. 飞行风险：`待核验 → 待通知 → 已通知`；待核验可转 `已排除`。`已通知/已排除` 保留现有终态语义；发送受理、送达、回执是独立通知记录的状态。真实渠道中何种结果允许进入“已通知”须在 Q4 确认，不能点击按钮即判成功，也不擅自增加“处置完成”风险终态。
3. 设备异常：原因与确认 → 下发重启 → 等待设备回执 → 平台恢复校验 → 关闭异常。前端摘要为 `待处理/处理中/待验证/已恢复`；设备在线、重启指令完成、异常恢复是不同事实，回执成功不直接等于平台恢复。

工作台可发起上述模块的动作，但不另建状态机。每个用例由后端统一检查当前状态、权限、前置条件及并发版本，成功后同步关联记录与审计；失败不得推进成功状态。

### 5.2 标识、来源与规则

- 平台业务主键、外部来源编号、展示编号、前端视图 key 分开。`uav:<targetId>/risk:<id>/device:<deviceId>` 不是持久化事件主键；每次设备故障有独立异常实例，历史记录不被下一次故障覆盖。
- 上报保留来源系统、来源 ID、接收时间与原始发生时间；跨来源 ID 不默认全局唯一。同目标告警的合并窗口、跨设备关联策略由 Q2/Q6 确认，不按空间接近自行合并。
- 数据分别标记真实上报、服务端推导、人工录入或演示来源。人工输入也需记录操作者和依据；推导结果留存输入依据和规则版本。
- 未知、不适用、缺失、过期应能区分，不以 0、合法、在线或成功替代未知。前端中的随机序列、固定演示时间、虚构文件哈希不得成为生产数据。
- 保留坐标系及高度基准。当前地图接入边界为 WGS-84，见[离线地图部署说明](离线地图部署说明.md)；业务接口与地图资源独立，不把旧高德代理作为后端建设前提。不明坐标系不静默转换，AGL 与 AMSL 缺少可靠转换依据时不得直接比较。
- 新增时间字段明确单位和时区，区分设备时间、接收时间、业务更新时间；业务时间通过后端统一时钟获取，测试可控。现有 epoch 毫秒字段保持兼容。
- `RISK_RULE` 高度余量、部分 `COMM_TH`、`EVID_RETAIN` 和演示处罚参数等不能未经确认用于生产判断。证据文件已按与 Demo `EVID_RETAIN` 同口径的**平台缺省**计算到期日，到期且未冻结可人工销毁，不自动清理；正式年限仍待 Q7。规则调整需有版本、影响范围及验证证据。

## 6. 开发顺序与基础加固

| 阶段 | 工作内容 | 退出条件 |
| --- | --- | --- |
| 1：基础加固与认证权限 | 环境隔离、会话/失败登录、权限与数据范围、审计语义、测试实际发现；随后贯通前端认证 | 真实后端会话链可验证，越权被拒绝，成功与失败审计可靠；不能以模拟用户切换替代认证 |
| 2：设备查询与异常闭环 | 台账、状态、异常实例、重启、回执及恢复验证；实现该切片需要的指令/幂等/任务能力 | 刷新及服务重启后状态可追溯；重复请求、超时和恢复失败可验证；模拟适配器明确隔离，模拟通过不算实联 |
| 3：告警与风险流程 | 两条独立状态机、确认后的授权和规则、交接通知 | 各页面读取同一状态；失败不推进，交接与处罚办结分开；依赖设备方的验收需真实材料 |
| 4：证据、工作台与统计 | 完整文件服务、跨域聚合、检索和统计导出 | 文件实际存在且受控，统计来源一致，跨区域查询与导出不越权 |

阶段是主交付顺序，不推迟前序业务必需的基础能力：阶段 2/3 已需要的真实材料关联、回执保存与审计须随切片完成，不能等阶段 4 才留证。

环境初始化已增加 `app.dev-seed.enabled` 显式开关：`local`/`test` profile 启用合成账号数据，默认环境和 `integration` profile 默认关闭。以下是其余静态识别的待验证/待修复事项，不表示本轮已复现或修复：

| 编号 | 发现与依据 | 后续验收重点 |
| --- | --- | --- |
| G2 | 失败登录计数与审计已改到独立事务 `[LoginFailureRecorder](../server/src/main/java/com/uav/lowaltitude/modules/identity/application/LoginFailureRecorder.java)` | 回归见 `LoginFailurePersistenceTest`；不能只断言 HTTP 401 |
| G3 | **已完成**（阶段 2 起）：动作级权限经 `[AccessControlService](../server/src/main/java/com/uav/lowaltitude/modules/identity/application/AccessControlService.java)` 与 `app_role_permission` 判定，数据范围经 `scope_mode` 与 `(owner_org_id, district_id)` 元组过滤，越权一律 404 不区分"不存在"与"不可见"；前后端共用同一套权限码 | 验收记录：[阶段 4 验收](backend-stage4/acceptance.md)（范围隔离由 `AlarmReadApiTest`/`RiskReadApiTest`/`Stage4PostgresTest` 的跨元组用例覆盖）。阶段 3 没有独立验收文档，只有契约。回归见各阶段的 `*AccessControlServiceTest` 与各读接口的越权用例；新增接口必须同时补越权用例 |
| G4 | **已完成**：`[OutboxWorker](../server/src/main/java/com/uav/lowaltitude/platform/worker/OutboxWorker.java)` 承担投递与重试，融合侧消费见 `FusionIngestWorker`，规则侧见 `RuleEngineWorker`；幂等经 `IdempotencyGuard`，重启恢复由 inbox 的 claim/租约保证 | 验收记录：[阶段 7 验收](backend-stage7/acceptance.md)（`RuleEngineWorker` 列入已交付的引擎组件）、[阶段 8 验收](backend-stage8/acceptance.md)（`inbox_message.fusion_attempts` 与领取上限、`Stage8PostgresTest` 的并发领取用例）。回归见 `DeviceOperationsApiTest`、`LingyunControlMqttTest` 与融合回放用例；新增 Worker 必须证明重复投递不产生第二条业务写入 |
| G5 | 认证测试已改名为 `AuthApiTest`，普通 `test` 可发现 | 核对 surefire 报告中的实际用例；失败登录落库见 G2 |
| G6 | F5 重启回执由约 2.6 秒定时器生成；F2 联动回执由约 3 秒定时器推进；F7 提交回调只传目标 | 接入时提交真实选择参数，服务端复核；回执必须来自隔离模拟器或认证后的真实来源 |
| G7 | `MockAdapter` 不再 `matchIfMissing`；`integration` 的 `APP_SOURCE_MODE` 必填；`SourceModeGuard` 在无对应 Adapter 时拒绝启动 | 未配置或 live 无适配器时启动失败，不得静默落到 Mock |

## 7. 待确认资料清单

“确认方”指需要提供资料的角色类别，不代表已有人员被指派；不设置虚构 SLA 或日期。材料可脱敏，不能提交真实凭据。

| 编号 | 问题/资料 | 确认方 | 影响模块 | 阻塞边界 |
| --- | --- | --- | --- | --- |
| Q1 | 设备接入/控制协议原件、版本、状态字典、回执及错误码、能力限制 | 设备方 | device/sensing/command/alarm/evidence | 阻塞真实设备接入；不阻塞接口抽象和隔离模拟验证 |
| Q2 | 脱敏报文与台账样例、来源 ID 唯一范围、时间单位、坐标系、高度基准、消息频率 | 设备方/数据提供方 | device/sensing/flight/assessment/alarm/risk | 阻塞对应来源的字段映射和实联验收 |
| Q3 | 真实组织、区域范围、岗位权限、敏感字段与身份接入方式 | 业务方/系统管理方 | 全部 | 阻塞生产账号初始化和权限验收；不阻塞安全框架实现 |
| Q4 | 计划/视频/上级/处罚系统接口、接收主体、鉴权、通知成功标准、回执和失败处理 | 对接单位/业务方 | flight/handoff/risk/evidence | 阻塞对应外部同步、通知成功判定及真实媒体验收 |
| Q5 | 反制/干扰/重启授权条件、审批依据、参数范围、停止/急停能力 | 业务方/设备方 | command/alarm/device | 阻塞真实受控指令启用，不能用演示双勾选替代生产授权 |
| Q6 | 告警合并规则、研判及调测阈值、风险参数、统计口径与规则变更机制 | 业务方/设备方 | sensing/assessment/alarm/risk/device/reporting | 阻塞相关生产自动判定及指标验收；未知规则保持待确认 |
| Q7 | 证据种类、真实文件来源、保留期限、冻结/销毁/导出制度和交接要求 | 业务方/档案管理方 | evidence/handoff/audit | 阻塞正式保管策略替换与自动清理；当前可人工销毁已到期且未冻结的文件 |
| Q8 | 部署网络、数据库要求、设备/用户/目标规模、轨迹频率、容量、备份恢复和验收环境 | 运维方/业务方/设备方 | 全部 | 阻塞生产容量、网络及运维验收；不阻塞本地开发 |

代码注释引用的《设备数据及感知数据接入协议 v8.6》、T02 雷达协议及设备规格书原件当前仓库检索未发现；注释中的出处不等于本轮已核验原件。资料未齐不阻塞基线建立，但必须阻塞相应真实接入或生产验收。

## 8. 后续开发验收门槛

- 每个切片先明确对象、来源、查询/写入契约、权限、合法迁移与错误场景，再写业务代码；所有 B/D 项保留完成或确认依据。
- 覆盖登录失败留痕及锁定、退出/过期/停用、动作和数据越权、敏感字段/导出控制，不能只测成功请求。
- 覆盖重复提交、并发操作、非法迁移、重复/迟到/超时回执、服务重启、恢复验证失败；确认状态、审计和关联材料一致。
- 覆盖未知数据、单位与基准不一致、证据缺失/哈希异常、外部接口失败；缺失信息不生成成功或合法结论。
- 区分 Mock、回放和真实来源；无真实配置不得悄悄降级或以模拟报告通过实联验收。
- H2 用于快速应用验证，不证明 PostgreSQL/PostGIS 的空间 SQL、约束、锁或迁移兼容。涉及这些能力时使用隔离真实数据库验证，不对生产库跑测试或迁移试验。
- 检查测试实际执行数量与报告，当前认证类显式指定；不得以构建退出成功、空列表或占位日志宣称功能完成。具体命令和按改动范围执行的要求见 [后端规则](../server/AGENTS.md)。

本轮文档验收仅核对源码事实、状态标记、引用与差异；不将编写规则视为已经实现规则。后续每次改变公共约定、流程或实现状态，应同步本基线和最近的接口/运行说明。


2026-09-15：新增业务前台设备异常通知 → 后台监测页运维待办。支持持久去重、范围控制与处理结果留痕；不改变设备恢复状态。接口和部署要求见[设备异常通知与运维待办](设备异常通知与运维待办.md)。代码已实现，按用户要求未构建、未测试、未运行数据库迁移。


2026-09-15 光电追踪补充：风险详情按服务端设备可用性开放追踪，已排除记录可单独观测；已知本地模拟来源按现有位置补全目标，缺位置不自动生成。新增可用性接口及未测试、未部署边界见[设备运维接口契约](设备运维接口契约.md)。
