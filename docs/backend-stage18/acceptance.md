# 阶段 18 验收记录：对照原版演示改回

日期 2026-09-10。基线 `8fad3ed`。对照报告（截图）：https://claude.ai/code/artifact/582fac70-809d-4b84-af57-431b4e0e93a6 。决策 18-1…18-16 见 `decisions.md`。

## 做了什么
- 18.1（E2）：告警内容一句人话 + 来源置信卡 + 筛选一行；飞行计划小地图比例尺；合法性页四格改回原版口径、页签统一"不可判定"、标题换行；角色页超管显示"全部"；风险页筛选收一行、地图默认全市；审计日志本机地址与模块别名。
- 18.2（E1）：四个演示角色 + 11 个演示账号种子（口令 changeme，周敏停用）；`AuditLabelsCoverageTest` 从源码扫审计码补 12 个中文。
- 18.4/18.5（E1）：角色权限按"菜单 → 页面所需读动作"表推导（`MENU_READS`）；值班员 `disposal:read`、复核员 `evidence:read`；审计模块允许授予非超管（18-13）。
- 18.5（E2）：审计页对无 `roles.read` 的角色不拉 `/roles`。
- 18.6（E1+E2，用户裁定 18-14）：风险到"通知上级"为止，回执"已驱离"即闭环；弹窗不选接收方、按钮"提交通知"；迁移 `V202609100101`（`handoff_recipient.is_default`、`handoff.receipt_result`）；接收方解析 默认 → 唯一启用 → 400（18-16）；回执 DISPERSED 时风险推进到"已通知"；处置主体名单去掉 RISK；驱鸟处置按钮删除。
- 18.7（E2，用户裁定 18-15）：合法性页删信息块、"转告警"、三个小按钮。
- 18.8 / 18.5 docs（助手、E2）：三份交付文档按现在页面逐字重核（界面上"尚未接入"已为 0）。
- 工作台排序"可操作 > 等级 > 时间"由另一会话在其文件里改回（18-1，不在本提交）。

## 验证
- H2 全量（E1，最终树）：956 例 0 红 / 106 skip。
- 真 PG（助手）：Stage5 5/5、Stage13 10/10、Stage15 9/9、Stage16 6/6；`HandoffReceiptResultApiTest` 1/1、`LocalDemoRolesSeederTest` 7/7 在 `stage456_verify_s18`（含 `db/postgresql` 触发器）上跑过。
- E2E 全角色矩阵 118/118 两遍（8081，干净包 09:09，`uav_stage10_verify` 升级路径），跑前跑后源码 mtime 与六账号权限快照一致；此前两对因 E1 补种子/E2 改页面而作废，六遍才取到可信一对。第一次跑 59 红是真发现（四个新角色重犯 15-34），修后清零。
- 联调（E2，8081 干净包 10:15）：风险-0907-003 核验通过 → 通知上级（无接收方下拉）→ 通报记录"已回执 · 已驱离" → 风险"已通知"，接口回读 NOTIFIED/DISPERSED。
- 升级路径：8081 两次用干净包起在 `uav_stage10_verify`（迁移 83 条，0101 已应用，默认接收方=民航监管部门，四个演示账号登录 200）。

## 未做 / 留待用户
- 融合感知页恢复原版面板、处置与处罚页改回案件为主（18-6，等用户点头）。
- A 的页面（设备管理排版、监测/调测空态、大屏两根柱、接收诊断英文码）已转 A（18-7）。
- 顶栏检索、方块小字、报表下载、实时视频/轨迹回放按钮不恢复（18-8）。

## CI
- 34548349393（`e28b353`）：三个作业全部 success——backend 955/0/0/0 跳 0，frontend 通过，e2e 118/118（全角色矩阵首次在 CI 上跑）。
