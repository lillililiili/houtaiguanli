# 电话录音通知实现与验收（2026-09-16）

本轮代码为短信之外补充电话录音通知。业务前台无人机事件的通知与现场核查位置可分别查看短信、电话录音状态；后端默认关闭电话功能。当前没有启用运行环境、没有拨打真实电话、没有生成业务录音、没有写入业务数据库。

## 触发与结果边界

- 触发数据：事件、已关联目标的当前观测、最新 ACTIVE 研判，以及必要时的近期人工确认；共享现有演示时效（目标/研判 120 秒，事件/确认 300 秒，可沿既有配置调整）。最新合法/未知/过期研判不回退旧确认；数据不足、现场未知或已飞离均阻断。
- 自动动作：独立数据库任务领取、稳定通道幂等键、事务外调用模拟端口、事务内原子保存结果/事件版本/审计。关闭页面不停止后台工作；GET 只读。
- 两种渠道独立防重，先短信或先电话都不压制另一通道。人工联系事实仍阻断重复自动通知。只有明确模拟接通并播完的回执写入成功电话联系记录。
- 人工介入：明确失败且当前触发、录音及权限仍满足时可申请补呼。调用结果未知、只有接通证据或租约过期都保留 UNKNOWN，需通道对账，当前没有对账接口，因此禁止补呼。补呼不改变录音内容且沿用原 provider_key。
- 历史事实：已接通/播完、任务来源及成功记录不因后来关闭配置、录音缺失或观测过期而消失。关闭策略后仅整理过期 CALLING，不新建或拨打。
- 录音来源：只读取部署端配置的已有绝对路径 WAV，校验元数据、真实可读音频帧及长度，实际计算 SHA-256；最大 10 MiB。没有用户录音资料时仍不可用。本轮没有增加上传、制作或管理录音功能。

## 接口

GET `/api/v1/uav-events/{eventId}/advisory` 新增 `voice_mode`、`auto_voice`，原 `auto_sms` 保持。`auto_voice` 在原短信摘要字段外增加 `recording_id`、`recording_name`、`answered_at`、`playback_completed_at`；无证据不返回时间。状态包括 DISABLED、WAITING、CALLING、SIMULATED_PLAYED、FAILED、UNKNOWN、UNAVAILABLE、BLOCKED。

POST `/api/v1/uav-events/{eventId}/advisory/auto-voice/retry` 接收 `{expected_version,note}` 与 Idempotency-Key，复用现有 alarm:read、alarm:verify、handoff:create 和事件范围。受理仅排队；同键同请求重放不重复操作。同键用于其他通道/事件/请求会冲突。

完成记录以 `VOICE_SIMULATED` 和 `SIMULATED_PLAYED` 合并到 `records`；后台模拟主体为“自动电话录音服务”，仅使用明确“演示飞手”接收端，没有真实手机号。后续新交接材料冻结这些联系事实，旧快照不回写。`voice_mode=SIMULATED` 保留历史尝试的来源，不能作为“当前已启用”的判断依据。

## 代码变更说明

下表只列本轮变更，不覆盖工作区已存在的自动短信、轨迹及其他并行改动。所有后端 Java 文件以下路径为起点：`server/src/main/java/com/uav/lowaltitude/`。

| 文件/类 | 方法/函数 | 方法作用 | 本次修改的代码作用 |
| --- | --- | --- | --- |
| modules/alarm/application/AdvisoryEligibilityService.java | evaluate、blocksChannel | 校验当前事实、来源关联和时效，按渠道检查已有记录 | 从短信提取共享规则，短信与电话不会互相压制 |
| modules/alarm/application/AutoSmsService.java | eligible | 委托共享规则，再校验短信适配器 | 保留短信现有契约与行为，复用资格判定 |
| modules/alarm/application/AutoVoicePolicy.java | enabled | 同时校验配置和 profile | 默认关闭；production 与 local/test 混用仍拒绝 |
| modules/alarm/application/AdvisoryVoiceRecording.java | current | 校验已有 WAV 的完整帧和实际哈希 | 不以空壳文件或元数据冒充可用录音 |
| modules/alarm/application/AdvisoryVoicePort.java | simulate | 电话通道接口与独立接通/播完回执 | 明确本轮模拟边界，未接真实供应商 |
| integration/mock/LocalAdvisoryVoiceAdapter.java | simulationAvailable、simulate | 限定环境/来源并回读已配置录音 | 只产生明确模拟回执，不拨号、不实际播放 |
| modules/alarm/application/AutoVoiceJob.java | poll | 后台定期调度 | 与页面/会话无关 |
| modules/alarm/application/AutoVoiceService.java | poll、process、claim、finish、overview、mode、retry | 任务领取、事务外调用、结果/审计保存、摘要与补呼守卫 | 双通道并行；未知不盲重试；保留部分接通和历史来源 |
| modules/alarm/infrastructure/AutoVoiceRepository.java | candidates、callingCandidates、find、initialize、claim、block、finish、queueRetry、append | 任务与成功录音通知记录持久化 | 独立任务键和表；关停后仍整理过期任务 |
| modules/alarm/api/UavAdvisoryDtos.java | 无具体方法，Overview/AutoVoice 字段 | 暴露渠道摘要与结果时间 | 增量契约保持旧字段兼容 |
| modules/alarm/api/UavAdvisoryController.java | retryVoice | 暴露补呼入口 | 与短信补发入口独立 |
| modules/alarm/application/UavAdvisoryService.java | view、retryAutomaticVoice | 范围/权限/版本/幂等校验和摘要组装 | 补呼仅排队，受后端权限约束 |
| modules/alarm/infrastructure/UavAdvisoryRepository.java | records | 合并两个记录表，按事件版本排序 | 现场核查与交接材料能消费电话成功事实 |
| modules/alarm/domain/UavAdvisoryRules.java | counterBlockReason | 保留后续现场核查与反制条件 | 提示文案包含电话录音劝离 |
| integration/mock/LocalAdvisorySmsAdapter.java | simulationAvailable | 明确禁止混合 production profile | 关闭与语音同一来源隔离边界的旁路 |
| server/src/main/resources/db/migration/V202609160002__automatic_advisory_voice.sql | 无具体方法，新建任务及记录表/约束 | 独立渠道持久化 | 不修改已应用迁移或旧历史 |
| server/src/test/java/.../AutoVoiceApiTest.java、AutoVoicePostgresTest.java、AutoVoiceRecordingTest.java、AdvisoryMigrationFixture.java | 对应业务测试 | API、并发、事务、权限、部分/未知回执、录音、恢复与升级校验 | 同一主链在 H2 与隔离 PostGIS 验证 |
| server/src/test/java/.../AutoSmsApiTest.java、AutoSmsPostgresTest.java、UavAdvisoryPostgresTest.java、LocalAdvisorySmsAdapterTest.java | 默认禁用、迁移升级计数、混合 profile 测试 | 原消费者回归 | 保留既有短信/联系流程 |

## 验收记录

最终结果已从 Surefire XML 核对：H2 71 项、PostgreSQL/PostGIS 50 项，合计 121 项，失败 0、错误 0、跳过 0。最终 `sh mvnw -q -DskipTests package` 退出 0。全部 16 个本轮生产代码/迁移文件与最终打包和 PG 测试源码副本逐文件一致。`git diff --check` 通过，新增文件另行检查无行尾空白。

| 环境 | 实际套件与数量 | 结果 |
| --- | --- | --- |
| H2 PostgreSQL 模式 | AutoVoiceApiTest 16、AutoVoiceRecordingTest 3、AutoSmsApiTest 18、AutoSmsPolicyTest 2、AutoSmsSchedulingTest 1、LocalAdvisorySmsAdapterTest 2、UavAdvisoryApiTest 13、HandoffPunishmentMaterialsApiTest 16 | 71 通过，0 失败/错误/跳过 |
| PostgreSQL 16.9 / PostGIS | AutoVoicePostgresTest 17、AutoSmsPostgresTest 19、UavAdvisoryPostgresTest 14 | 50 通过，0 失败/错误/跳过 |
| 构建 | `sh mvnw -q -DskipTests package` | 退出 0；构建不启动服务 |

实际命令（在各自复制出的 `server/` 目录内执行）：

```sh
sh mvnw -q -Dtest=AutoVoiceApiTest,AutoVoiceRecordingTest,AutoSmsApiTest,AutoSmsPolicyTest,AutoSmsSchedulingTest,LocalAdvisorySmsAdapterTest,UavAdvisoryApiTest,UavAdvisoryRulesTest,HandoffPunishmentMaterialsApiTest test
# 实际发现并执行的 8 个套件及数量以上表为准；规则行为由 API 回归覆盖。
sh mvnw -q -Dtest=AutoVoicePostgresTest,AutoSmsPostgresTest,UavAdvisoryPostgresTest test
sh mvnw -q -DskipTests package
```

PG 命令由 `POSTGRES_TEST_URL/USER/PASSWORD` 注入独立测试库配置；文档及日志不包含凭据。库先配置 `public` schema 的 PostGIS 扩展。分别从 `202609150005`、`202609150007`、`202609160001` 升级至源码最新版本，核实 `202609160002` 成功应用；每条路径在升级前实际插入一条历史模拟短信，升级后逐字段核对原内容、发生时间、操作者及关联，并验证 `validate()` 成功、再次 `migrate()` 为 0、没有伪造电话任务。本次源码还包含其他并行任务的 `202609160004`，升级测试按实际版本存在与历史保持验收，不依赖最新迁移总数量。

所有数据库操作限定本轮新建的临时 `advisory_verify_` 测试库。最终库 `advisory_verify_voice_cf77191439ef` 已删除；前两次排查使用的两个独立库也已删除，最后只读查询确认这 3 个库剩余数量为 0。未修改业务库、未重启共享服务。Maven 最终在 `/tmp/dongying-voice-backend-verify/server` 和 `/tmp/dongying-voice-pg-verify/server` 中执行，避免多个任务共享 target 干扰。

初轮验证中已处理：共享构建目录并发产物竞争；测试审计 stub 误经 Spring MANDATORY 事务代理；新验收库未预建 PostGIS 扩展；并行新增迁移使旧固定数量断言失效。上述均已定位并按最终命令重新验证通过，未跳过失败测试。独立审阅提出的截断 WAV、部分接通事实、禁用后租约恢复、历史模拟标签均已修复并有回归断言。

业务前台的页面、组件与浏览器验收另见[电话录音通知前台验收](../../dongyiwurenji/docs/backend-stage19/uav-voice-notification.md)。

测试录音为用例临时生成的明确静音 WAV 格式样本，只用于验证格式和模拟状态机，不是业务通知录音，不进入业务目录或数据库。未添加依赖、未修改 application-local.yml 自动启用电话。

## 剩余限制

正式电话通知仍需供应商协议/幂等支持、授权且可追溯的号码来源、可信回调认证、接通/播完回执与对账，以及用户实际录音。没有这些资料时保持不可用。本轮模拟回执不表示实际拨号、实际播放、真实接听或目标已飞离；不会自动批准反制或处罚。管理端 ruoyi-ui/src 未发现 advisory 直接消费者，无需添加管理菜单。
