# 新后端迁移记录

2026-09-14。用户要求停止复用旧工作区后端，将旧后端修改迁入当前仓库。当前独立运行入口为 `houtaiguanli/server`，local API 为 `http://127.0.0.1:8081`，管理端为 `http://127.0.0.1:5175`。

## 合并依据

逐文件比较旧工作区 `/Users/frank/Desktop/dongyiwurenji/server` 与新仓库，使用旧仓库 HEAD 作为三方合并基线。初始发现 555 个文件相同、31 个旧后端独有文件、57 个共同路径内容不同；仅旧侧发生的修改迁入，共同修改逐项合并，新侧独有功能保留。读取源码与版本记录，没有复制旧 `.env.local`、凭据、构建物或运行数据。

迁入目标/轨迹读取、飞行实际轨迹、系统设备检查与回告、天气风险、空域提前结束、处置事件次序、权限枚举、CSV 中文标签和本地模拟工具。保留新仓库的设备删除、完整调测信息、地图包管理、统计导出及现有身份管理改动。处置链冲突两侧都是批准事件晚于申请 1ms，合并为命名时间变量，未扩大控制能力。

`.gitignore` 原有 `**/target/` 同时排除了 Java 业务包 `modules/target`，补充源码与测试路径例外，构建目录 `server/target` 仍忽略。目标模块及其测试现在可以纳入版本管理。

## 迁移历史与运行隔离

| 旧侧来源 | 新仓库落点 | 处理 |
| --- | --- | --- |
| `V202609140001__flight_verification_feedback.sql` | `V202609140010__flight_verification_feedback.sql` | 作为新导入迁移追加，SQL 内容保持一致，避免与新仓库设备删除 001 冲突。 |
| `V202609140002__flight_automatic_check.java` | `V202609140011__flight_automatic_check.java` | 新迁移类按新版本命名；保留迁移逻辑，避免与设备调测信息 002 冲突。 |
| `V202609140003__weather_risk_map.sql` | 同名新增 | 无版本冲突。 |
| 旧 `R__stage9_airspace_succession.sql` 中提前结束规则 | `R__stage9_airspace_version_closing_function.sql` | 新增只定义函数的重复迁移，排序在原脚本之后；原有已应用脚本不改，首次安装和后续升级都应用新函数。 |

没有修改旧工作区的迁移文件或校验历史，也没有改写新仓库此前已有的 SQL 文件。新后端使用独立 `houtaiguanli` 数据库，由 Flyway 从当前合并源码初始化；旧 `uav` 数据不自动转移，两边会话不通用。只迁代码不迁旧业务数据，避免把迁移版本冲突掩盖成已应用。

local 默认端口改为 8081，Vite `/dev-api` 代理指向 8081，Compose 外部 API 默认映射同为 8081（容器内部仍为 8080）。local 地图数据默认存于自身 `server/data/map-data`，可用 `MAP_DATA_DIR` 显式覆盖。Windows 初始化脚本为已有数据卷补建专用数据库；健康检查默认检查新后端与管理端，业务前台用 `-CheckBusinessFrontend` 显式加入。未更改旧工作区前端配置或停止旧后端进程。

## 代码变更说明

表中列出本次迁入或修改的代码/配置；源码完整复制的类保留其所有原方法，方法列归组列出主要入口。资料与测试不代表已接入现场设备。

| 改动文件 / 类 | 改动方法 / 函数 | 方法作用与本次修改的代码作用 |
| --- | --- | --- |
| `server/scripts/keepalive_lingyun_static.py` | load(), is_static(), is_eo_heartbeat(), stamped(), parse_args(), main() | 迁入本地 MQTT 回放、工参保活和检查演示工具。 |
| `server/scripts/simulate_flight_check_mqtt.py` | main() | 迁入本地 MQTT 回放、工参保活和检查演示工具。 |
| `server/src/main/java/com/uav/lowaltitude/integration/mock/LocalFlightDeviceMqttSeeder.java` | run() | 迁入或合并本地模拟/种子逻辑，保留 local/test 与生产隔离。 |
| `server/src/main/java/com/uav/lowaltitude/integration/mock/LocalFlightPathDemoSeeder.java` | prefix(), run() | 迁入或合并本地模拟/种子逻辑，保留 local/test 与生产隔离。 |
| `server/src/main/java/com/uav/lowaltitude/integration/mock/LocalPendingPlanDemoSeeder.java` | run() | 迁入或合并本地模拟/种子逻辑，保留 local/test 与生产隔离。 |
| `server/src/main/java/com/uav/lowaltitude/integration/mock/LocalRiskVideoEoSimulator.java` | keepOnline(), connectionLost(), deliveryComplete(), messageArrived(), shutdown() | 迁入或合并本地模拟/种子逻辑，保留 local/test 与生产隔离。 |
| `server/src/main/java/com/uav/lowaltitude/integration/mock/LocalWeatherRiskSeeder.java` | run() | 迁入或合并本地模拟/种子逻辑，保留 local/test 与生产隔离。 |
| `server/src/main/java/com/uav/lowaltitude/modules/flight/api/FlightTrajectoryController.java` | trajectory() | 迁入飞行实际轨迹、设备系统检查、独立核验事实与来源回告流程。 |
| `server/src/main/java/com/uav/lowaltitude/modules/flight/api/FlightVerificationController.java` | read(), check(), automatic(), verify(), feedback() | 迁入飞行实际轨迹、设备系统检查、独立核验事实与来源回告流程。 |
| `server/src/main/java/com/uav/lowaltitude/modules/flight/api/FlightVerificationDtos.java` | VerifyRequest(), AutomaticRequest(), FeedbackRequest(), Verification(), Feedback(), Workflow() | 迁入飞行实际轨迹、设备系统检查、独立核验事实与来源回告流程。 |
| `server/src/main/java/com/uav/lowaltitude/modules/flight/application/FlightDeviceCheckService.java` | DeviceRow(), Check(), read() | 迁入飞行实际轨迹、设备系统检查、独立核验事实与来源回告流程。 |
| `server/src/main/java/com/uav/lowaltitude/modules/flight/application/FlightTrajectoryService.java` | Point(), Trajectory(), read() | 迁入飞行实际轨迹、设备系统检查、独立核验事实与来源回告流程。 |
| `server/src/main/java/com/uav/lowaltitude/modules/flight/application/FlightVerificationService.java` | read(), verify(), deviceCheck(), automatic(), feedback() | 迁入飞行实际轨迹、设备系统检查、独立核验事实与来源回告流程。 |
| `server/src/main/java/com/uav/lowaltitude/modules/flight/infrastructure/FlightVerificationRepository.java` | lockPlan(), verifications(), feedback(), sourceEnabled(), insert() | 迁入飞行实际轨迹、设备系统检查、独立核验事实与来源回告流程。 |
| `server/src/main/java/com/uav/lowaltitude/modules/risk/api/WeatherRiskDto.java` | WeatherRiskDto() | 迁入天气风险读取及空间风险修正。 |
| `server/src/main/java/com/uav/lowaltitude/modules/risk/infrastructure/WeatherRiskRepository.java` | FactRow(), find(), insertDemo() | 迁入天气风险读取及空间风险修正。 |
| `server/src/main/java/com/uav/lowaltitude/modules/target/api/TargetDtos.java` | LocationDto(), FieldIssueDto(), TargetStateDto(), RiskSummaryDto(), LegalitySummaryDto(), DisposalSummaryDto(), TargetSummaryDto(), TargetDetailDto() 等 | 恢复目标详情、摘要和轨迹读取，补齐新后端缺失的编译与运行依赖。 |
| `server/src/main/java/com/uav/lowaltitude/modules/target/api/TargetReadController.java` | targets(), target(), tracks(), points() | 恢复目标详情、摘要和轨迹读取，补齐新后端缺失的编译与运行依赖。 |
| `server/src/main/java/com/uav/lowaltitude/modules/target/application/TargetReadService.java` | targets(), target(), tracks(), points() | 恢复目标详情、摘要和轨迹读取，补齐新后端缺失的编译与运行依赖。 |
| `server/src/main/java/com/uav/lowaltitude/modules/target/infrastructure/TargetReadRepository.java` | countTargets(), listTargets(), findTarget(), sourceLinks(), countTracks(), listTracks(), accessibleValidTrack(), countPoints() 等 | 恢复目标详情、摘要和轨迹读取，补齐新后端缺失的编译与运行依赖。 |
| `server/src/main/java/db/migration/V202609140011__flight_automatic_check.java` | migrate() | 在新后端迁移链中加入迁入业务表或函数，避开既有版本。 |
| `server/src/main/resources/db/migration/V202609140010__flight_verification_feedback.sql` | 无具体方法：声明、DTO 或资源定义 | 在新后端迁移链中加入迁入业务表或函数，避开既有版本。 |
| `server/src/main/resources/db/migration/V202609140003__weather_risk_map.sql` | 无具体方法：声明、DTO 或资源定义 | 在新后端迁移链中加入迁入业务表或函数，避开既有版本。 |
| `server/src/test/java/com/uav/lowaltitude/integration/mock/LocalPendingPlanDemoSeederTest.java` | 无具体方法：声明、DTO 或资源定义 | 回归迁入行为；飞行核验测试改走自动检查，H2 使用设备位置未知场景，PostgreSQL 保留实际空间路径。 |
| `server/src/test/java/com/uav/lowaltitude/modules/flight/api/FlightPathDemoPostgresTest.java` | 无具体方法：声明、DTO 或资源定义 | 回归迁入行为；飞行核验测试改走自动检查，H2 使用设备位置未知场景，PostgreSQL 保留实际空间路径。 |
| `server/src/test/java/com/uav/lowaltitude/modules/flight/api/FlightVerificationApiTest.java` | 无具体方法：声明、DTO 或资源定义 | 回归迁入行为；飞行核验测试改走自动检查，H2 使用设备位置未知场景，PostgreSQL 保留实际空间路径。 |
| `server/src/test/java/com/uav/lowaltitude/modules/flight/api/FlightVerificationPostgresTest.java` | 无具体方法：声明、DTO 或资源定义 | 回归迁入行为；飞行核验测试改走自动检查，H2 使用设备位置未知场景，PostgreSQL 保留实际空间路径。 |
| `server/src/test/java/com/uav/lowaltitude/modules/flight/application/FlightTrajectoryServiceTest.java` | 无具体方法：声明、DTO 或资源定义 | 回归迁入行为；飞行核验测试改走自动检查，H2 使用设备位置未知场景，PostgreSQL 保留实际空间路径。 |
| `server/src/test/java/com/uav/lowaltitude/modules/target/api/TargetReadApiTest.java` | 无具体方法：声明、DTO 或资源定义 | 回归迁入行为；飞行核验测试改走自动检查，H2 使用设备位置未知场景，PostgreSQL 保留实际空间路径。 |
| `server/src/test/java/com/uav/lowaltitude/modules/target/api/TargetReadPostgresApiTest.java` | 无具体方法：声明、DTO 或资源定义 | 回归迁入行为；飞行核验测试改走自动检查，H2 使用设备位置未知场景，PostgreSQL 保留实际空间路径。 |
| `server/src/test/java/com/uav/lowaltitude/modules/target/api/TargetSummariesApiTest.java` | 无具体方法：声明、DTO 或资源定义 | 回归迁入行为；飞行核验测试改走自动检查，H2 使用设备位置未知场景，PostgreSQL 保留实际空间路径。 |
| `server/scripts/publish_lingyun_ndjson.py` | mqtt_client(), load_records(), parse_args(), rewrite(), main() | 迁入本地 MQTT 回放、工参保活和检查演示工具。 |
| `server/scripts/reply_lingyun_control.py` | parse_args(), reply_topic(), response_bytes(), main() | 迁入本地 MQTT 回放、工参保活和检查演示工具。 |
| `server/src/main/java/com/uav/lowaltitude/integration/mock/LocalMqttSimSeeder.java` | LingyunDevice(), run() | 迁入或合并本地模拟/种子逻辑，保留 local/test 与生产隔离。 |
| `server/src/main/java/com/uav/lowaltitude/integration/mock/LocalStage3PlanningSeeder.java` | run() | 迁入或合并本地模拟/种子逻辑，保留 local/test 与生产隔离。 |
| `server/src/main/java/com/uav/lowaltitude/integration/mock/LocalStage8FusionReplaySeeder.java` | SourceSeed(), run(), drain() | 迁入或合并本地模拟/种子逻辑，保留 local/test 与生产隔离。 |
| `server/src/main/java/com/uav/lowaltitude/modules/airspace/application/AirspaceWriteService.java` | create(), addVersion() | 迁入空域提前结束及相关写入守卫。 |
| `server/src/main/java/com/uav/lowaltitude/modules/airspace/infrastructure/AirspaceWriteRepository.java` | postgis(), AirspaceHead(), VersionRow(), lockAirspace(), findAirspace(), scopeGranted(), airspaceNoExists(), findAirspaceIdByNo() 等 | 迁入空域提前结束及相关写入守卫。 |
| `server/src/main/java/com/uav/lowaltitude/modules/disposal/application/DisposalJammingChain.java` | scheduleAfterComplete(), afterCommit() | 保留处置规则与确定的事件时间顺序。 |
| `server/src/main/java/com/uav/lowaltitude/modules/disposal/domain/DisposalRules.java` | outcomeOf(), requireTransition(), requireTwoPerson(), requireWithinWindow(), expired(), allowedActions(), deviceStopResult(), executionBlockReason() 等 | 保留处置规则与确定的事件时间顺序。 |
| `server/src/main/java/com/uav/lowaltitude/modules/flight/infrastructure/FlightActualsRepository.java` | findLatestActiveEvaluation(), trackGapMillis(), EvaluationRow() | 迁入飞行实际轨迹、设备系统检查、独立核验事实与来源回告流程。 |
| `server/src/main/java/com/uav/lowaltitude/modules/fusion/application/FusionProperties.java` | isEnabled(), setEnabled(), getPollMillis(), setPollMillis(), getBatchSize(), setBatchSize(), getLeaseMillis(), setLeaseMillis() 等 | 迁入旧侧融合配置默认行为。 |
| `server/src/main/java/com/uav/lowaltitude/modules/handoff/infrastructure/MockSuperiorHandoffChannel.java` | simulated(), deliver() | 迁入本地模拟回告行为。 |
| `server/src/main/java/com/uav/lowaltitude/modules/identity/domain/PermissionCode.java` | value() | 增加飞行核验动作，保留新仓库既有权限枚举。 |
| `server/src/main/java/com/uav/lowaltitude/modules/risk/api/RiskController.java` | list(), districts(), export(), detail(), history(), weatherFact(), verify() | 迁入天气风险读取及空间风险修正。 |
| `server/src/main/java/com/uav/lowaltitude/modules/risk/application/RiskReadService.java` | list(), districts(), export(), detail(), weatherFact(), dto(), id(), notFound() | 迁入天气风险读取及空间风险修正。 |
| `server/src/main/java/com/uav/lowaltitude/modules/risk/application/spacerisk/SpaceRiskEvaluationService.java` | evaluate() | 迁入天气风险读取及空间风险修正。 |
| `server/src/main/java/com/uav/lowaltitude/platform/export/CsvLabels.java` | severity(), alarmType(), uavEventState(), riskType(), riskState() | 迁入导出标签修正。 |
| `server/src/main/resources/application-local.yml` | 无具体方法：声明、DTO 或资源定义 | 合并旧侧业务配置并保留新侧配置；本地服务使用独立端口、数据库与目录。 |
| `server/src/main/resources/application.yml` | 无具体方法：声明、DTO 或资源定义 | 合并旧侧业务配置并保留新侧配置；本地服务使用独立端口、数据库与目录。 |
| `server/src/test/java/com/uav/lowaltitude/integration/mock/LocalStage3PlanningSeederTest.java` | 无具体方法：声明、DTO 或资源定义 | 回归迁入行为；飞行核验测试改走自动检查，H2 使用设备位置未知场景，PostgreSQL 保留实际空间路径。 |
| `server/src/test/java/com/uav/lowaltitude/integration/mock/LocalStage9SpaceRiskSeederTest.java` | 无具体方法：声明、DTO 或资源定义 | 回归迁入行为；飞行核验测试改走自动检查，H2 使用设备位置未知场景，PostgreSQL 保留实际空间路径。 |
| `server/src/test/java/com/uav/lowaltitude/modules/airspace/api/AirspaceWriteApiTest.java` | 无具体方法：声明、DTO 或资源定义 | 回归迁入行为；飞行核验测试改走自动检查，H2 使用设备位置未知场景，PostgreSQL 保留实际空间路径。 |
| `server/src/test/java/com/uav/lowaltitude/modules/airspace/api/Stage9PostgresTest.java` | 无具体方法：声明、DTO 或资源定义 | 回归迁入行为；飞行核验测试改走自动检查，H2 使用设备位置未知场景，PostgreSQL 保留实际空间路径。 |
| `server/src/test/java/com/uav/lowaltitude/modules/risk/api/RiskListSortExportApiTest.java` | 无具体方法：声明、DTO 或资源定义 | 回归迁入行为；飞行核验测试改走自动检查，H2 使用设备位置未知场景，PostgreSQL 保留实际空间路径。 |
| `server/src/main/resources/db/postgresql/R__stage9_airspace_version_closing_function.sql` | 无具体方法：声明、DTO 或资源定义 | 在新后端迁移链中加入迁入业务表或函数，避开既有版本。 |
| `.gitignore` | 无具体方法：配置块 | 修复源码忽略规则。 |
| `ruoyi-ui/vite.config.js` | 无具体方法：配置块 | 使新后端独立启动与管理端代理、初始化/健康检查一致。 |
| `deploy/compose.yml` | 无具体方法：配置块 | 使新后端独立启动与管理端代理、初始化/健康检查一致。 |
| `scripts/bootstrap-dev.ps1` | 初始化流程 | 使新后端独立启动与管理端代理、初始化/健康检查一致。 |
| `scripts/verify-dev.ps1` | Test-Endpoint() / 验证流程 | 使新后端独立启动与管理端代理、初始化/健康检查一致。 |

## 验证记录

- 后端完整编译、测试源码编译与可执行 JAR 打包已恢复。
- 新后端在自身工作目录启动，Flyway 验证 92 项迁移，独立 PostgreSQL 16/PostGIS 3.5 数据库初始化成功。
- 最终构建包重启通过：8081 监听进程工作目录确认为 `/Users/frank/Desktop/houtaiguanli/server`；Flyway 再次校验 92 项迁移，数据库已为最新版本。健康检查 UP，管理端代理登录和 28 台设备完整信息读取再次通过。
- 经管理端代理登录新会话后，28 台设备的完整信息接口及目标查询均返回成功；检查会话随后退出，不保存凭据。
- 前端 lint、24 项测试与构建通过。
- 迁入的旧飞行测试此前调用已禁用的人工结论入口，本次调整到 `/verifications/automatic`；H2 使用位置未知的合法设备夹具，不作为空间链验收依据。最终 Maven `package` 含 109 项受影响测试全部通过，0 失败、0 错误、0 跳过；包含真实 PostgreSQL 飞行核验和 PostGIS 空域提前结束约束测试。不是声称已运行全部 181 个测试类。
- 独立验证库为 `stage_flight_verify_transfer_20260914` 和 `stage456_verify_transfer_20260914`，与新服务库、旧服务库均分开；测试完成后已清理，运行库 `houtaiguanli` 保留。
- Windows PowerShell 脚本未在本机 macOS 实际执行，已同步命令与配置；本机启动、登录和 API 代理通过实际 HTTP 检查。
- 旧库历史业务数据、现场设备连通与真实控制效果不在本次代码迁移验收内。


## 2026-09-15 剩余旧工作区改动补迁

此前迁移未包含旧工作区最后追加的事件急停、飞行备案/演示补充、待执行计划设备预检和risk_types筛选。本次已补齐，76个旧源文件逐项核对结果、版本映射、代码说明及测试记录见[完整迁移清单](旧后端剩余改动迁移清单-20260915.md)。追加迁移为150002/150003/150004；保留设备维护150001和既有较新实现。测试包含87项不同环境用例，最终相关package通过；不是全量测试结论。首次原目录构建触发现有DevTools自动重启并应用新迁移，此后验证改在临时源码副本和独立PG测试库完成。
